# Interface graphique : controles Win32 natifs, lus nativement par NVDA.
wxPython>=4.2.1

# SSH pur Python (palier 2). Installe des maintenant pour eviter une
# seconde passe d'installation plus tard.
paramiko>=3.4

# Stockage des mots de passe dans le Gestionnaire d'identifiants Windows.
keyring>=25.0

# Production de l'executable final (palier 3).
pyinstaller>=6.3
